import React from 'react';
import { StyledMainMenu } from './styles';

const MainMenu = () => {
    return (
        <StyledMainMenu>
            Main Menu
        </StyledMainMenu>
    );
};

export default MainMenu;