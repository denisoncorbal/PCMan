import React from 'react';
import { StyledSideContent } from './styles';

const SideContent = () => {
    return (
        <StyledSideContent>
            Aside
        </StyledSideContent>
    );
};

export default SideContent;