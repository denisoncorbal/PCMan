import React from 'react';
import { StyledMainContent } from './styles';

const MainContent = () => {
    return (
        <StyledMainContent>
            Main
        </StyledMainContent>
    );
};

export default MainContent;