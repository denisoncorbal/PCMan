import React from 'react';
import { StyledFooter } from './styles';

const Footer = () => {
    return (
        <StyledFooter>
            Links --- Copyright
        </StyledFooter>
    );
};

export default Footer;