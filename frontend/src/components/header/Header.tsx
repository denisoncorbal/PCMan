import React from 'react';
import { StyledHeader } from './styles';

const Header = () => {
    return (
        <StyledHeader>
            Logo --- Profile
        </StyledHeader>
    );
};

export default Header;